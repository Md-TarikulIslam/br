import React from "react";
import navbar from "../assests/images/navbar.png";

const Navbar = () => {
    return (
        <nav>
            <img className="w-full" src={navbar} alt="" />
        </nav>
    );
};

export default Navbar;
